# Smoke Tests

Fast checks suitable for every packaged build and deployment.

Planned checks include process startup, readiness, HTTP API reachability, DNS A/AAAA resolution, Local DNS, blocklist behavior, frontend asset serving, and required packaged runtime directories.

The existing PowerShell DNS smoke script remains the operational smoke check until a cross-platform harness is added.
