# Test Fixtures

Reusable deterministic inputs for DNS records/zones, blocklists, API payloads, configuration, and seeded SQLite state.

Keep normal fixtures small. Large generated datasets belong with load-test tooling rather than the normal correctness-test fixture set.
