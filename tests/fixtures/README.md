# Test Fixtures

Reusable, deterministic inputs for tests.

Examples include DNS zone/record data, blocklist datasets, API payloads, configuration files, and seeded SQLite data.

Fixtures should be small by default. Large datasets used only for performance testing belong with the load-test tooling and should be generated or documented rather than silently checked in.
