{
  "name": "mydns",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace mydns-dashboard",
    "typecheck": "npm run typecheck --workspace mydns-dashboard",
    "build": "npm run build --workspace mydns-dashboard"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
