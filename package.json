{
  "name": "mydns",
  "private": true,
  "workspaces": [
    "src/frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @mydns/frontend",
    "typecheck": "npm run typecheck --workspace @mydns/frontend",
    "build": "npm run build --workspace @mydns/frontend"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
