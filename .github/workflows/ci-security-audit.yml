name: CI Capability - Security Audit
on:
  workflow_call:
jobs:
  audit:
    name: Security (Linux) - Audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
      - uses: taiki-e/install-action@cargo-audit
      - run: cargo audit
