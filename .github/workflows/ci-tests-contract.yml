name: CI Capability - Contract Tests
on:
  workflow_call:
jobs:
  contract:
    name: Tests - Contract
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Validate API contract
        run: python tests/contract/api_contract.py
