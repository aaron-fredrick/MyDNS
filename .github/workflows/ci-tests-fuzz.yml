name: CI Capability - Fuzz Tests
on:
  workflow_dispatch:
jobs:
  fuzz:
    name: Fuzz tests (placeholder)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Placeholder
        run: echo "Fuzz testing is reserved for manual execution. No fuzz suite is enabled yet."
